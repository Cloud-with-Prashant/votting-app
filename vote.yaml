apiVersion: v1
kind: Pod
metadata:
  name: vote
  labels:
    app: votting-app
    tier: vote-front
spec:
  containers:
    - name: vote
      image: kodekloud/voting-app
      ports:
        - containerPort: 80
          